[package]
name = "tracker_test"
version = "0.1.0"
edition = "2021"

[dependencies]
tracker_core = {path = "../core"}
time = { version = "0.3.9", features = ["serde", "macros"] }
anyhow = "1.0"
pretty_env_logger = "0.4.0"
rand = "^0.8"